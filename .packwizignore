# --- Системные и Git файлы ---
.git/
.gitignore
.packwizignore

# --- Файлы лаунчера (CurseForge) ---
minecraftinstance.json

# --- Локальные данные игрока (у каждого должны быть свои) ---
saves/
logs/
local/
downloads/
usercache.json
usernamecache.json
options.txt

# --- Карты и вейпоинты (чтобы игроки не видели твою карту) ---
journeymap/
xaero/

# --- Бэкапы (они только всё ломают) ---
XaeroWaypoints_BACKUP*/
tacz_backup/

# --- Игнорируем тяжелые архивы (оставляем только .pw.toml) ---
mods/*.jar
resourcepacks/*.zip
shaderpacks/*.zip
shaderpacks/*.txt